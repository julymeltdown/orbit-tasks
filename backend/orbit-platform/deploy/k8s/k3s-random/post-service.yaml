apiVersion: apps/v1
kind: Deployment
metadata:
  name: post-service
  namespace: boilerplate-random
spec:
  replicas: 1
  selector:
    matchLabels:
      app: post-service
  template:
    metadata:
      labels:
        app: post-service
    spec:
      containers:
        - name: post-service
          image: ${POST_IMAGE}
          imagePullPolicy: IfNotPresent
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: local
            - name: POST_GRPC_STUB_ENABLED
              value: "false"
            - name: POST_SEED_ENABLED
              value: "true"
            - name: POST_SEED_RESET
              value: "true"
            - name: POST_REDIS_ENABLED
              value: "true"
            - name: POST_PERSISTENCE_MODE
              value: "jpa"
            - name: REDIS_URL
              value: redis://redis:23111
            - name: DB_URL
              value: jdbc:postgresql://post-postgres:25477/post_db
            - name: DB_USERNAME
              value: post_user
            - name: DB_PASSWORD
              value: post_pass
            - name: POST_KAFKA_ENABLED
              value: "true"
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: kafka:22391
            - name: JPA_DDL_AUTO
              value: "update"
            - name: POST_EVENTS_REPUBLISH_ENABLED
              value: "true"
            - name: POST_EVENTS_REPUBLISH_INTERVAL
              value: "PT5M"
            - name: POST_EVENTS_REPUBLISH_DELAY
              value: "PT5M"
            - name: POST_REDIS_FEED_MAX_SIZE
              value: "500"
            - name: POST_REDIS_FEED_TTL
              value: "PT6H"
            - name: POST_REDIS_POST_TTL
              value: "PT12H"
            - name: SERVER_PORT
              value: "26809"
            - name: GRPC_SERVER_PORT
              value: "29567"
            - name: AUTH_JWKS_URL
              value: http://auth-service:21937/.well-known/jwks.json
            - name: POST_INTERNAL_SERVICE_TOKEN
              value: internal-service-token
            - name: PROFILE_GRPC_TARGET
              value: static://profile-service:27183
            - name: FRIEND_GRPC_TARGET
              value: static://friend-service:29921
            - name: NOTIFICATION_GRPC_TARGET
              value: static://notification-service:21463
          ports:
            - name: http
              containerPort: 26809
            - name: grpc
              containerPort: 29567
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: post-service
  namespace: boilerplate-random
spec:
  selector:
    app: post-service
  ports:
    - name: http
      port: 26809
      targetPort: http
    - name: grpc
      port: 29567
      targetPort: grpc
