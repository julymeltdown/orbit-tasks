{
  "name": "orbit-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "playwright test -c playwright.config.ts",
    "test:headed": "playwright test -c playwright.config.ts --headed",
    "test:visual": "playwright test -c playwright.config.ts visual/orbit-visual-baseline.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}

